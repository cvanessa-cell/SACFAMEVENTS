/**
 * Responsible for fetching upstream HTML/Markdown, chunking content, calling OpenAI, and hydrating Airtable records.
 */

export async function extractEventsFromPages(): Promise<number> {
  return 0;
}
