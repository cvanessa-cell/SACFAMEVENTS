/**
 * Skeleton hook points for injecting a panel into Google Calendar.
 * Google’s SPA mutates aggressively; richer automation ships after UX research.
 */

console.debug("[FFB] Sacramento Family Finder content script standby");
